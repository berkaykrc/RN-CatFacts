import { StylesSheet } from 'react-native';

const catfactcard_style = StylesSheet.create({
    container: {
        backgroundColor: 'white',
        padding: 10,
        margin: 10,
        borderRadius: 10,
    },
    title: {
        fontSize: 25,
        fontWeight: 'bold',
        textAlign: 'center',
    },
})

export { catfactcard_style }